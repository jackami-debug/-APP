# 自啟動與後台執行權限功能

## 概述

為了確保應用在設備重啟後能自動啟動，並在後台持續運行而不被系統關閉，我們添加了以下功能：

## 新增功能

### 1. 自啟動權限
- **功能**：允許應用在設備開機後自動啟動
- **實現**：
  - 添加了 `BootReceiver` 接收器，監聽開機完成事件
  - 在 `AndroidManifest.xml` 中註冊了 `RECEIVE_BOOT_COMPLETED` 權限
  - 支援多種廠商系統（小米、華為、OPPO、vivo、三星等）

### 2. 後台執行權限
- **功能**：防止系統在背景中關閉應用
- **實現**：
  - 添加了 `BackgroundService` 前台服務
  - 請求忽略電池優化權限
  - 使用 WakeLock 保持服務運行
  - 定期檢查服務狀態並自動重啟

### 3. 權限管理工具
- **功能**：統一管理各種權限的檢查和請求
- **實現**：
  - `PermissionManager` 工具類
  - 自動檢測不同廠商的系統
  - 提供權限狀態檢查和請求方法

## 新增文件

### 1. `BootReceiver.kt`
```kotlin
// 位置：app/src/main/java/com/example/appenergytracker/receiver/BootReceiver.kt
// 功能：開機自啟動接收器
```

### 2. `BackgroundService.kt`
```kotlin
// 位置：app/src/main/java/com/example/appenergytracker/service/BackgroundService.kt
// 功能：後台服務，確保應用持續運行
```

### 3. `PermissionManager.kt`
```kotlin
// 位置：app/src/main/java/com/example/appenergytracker/util/PermissionManager.kt
// 功能：權限管理工具類
```

## 修改文件

### 1. `AndroidManifest.xml`
- 添加了自啟動和後台執行相關權限
- 註冊了 `BootReceiver` 和 `BackgroundService`

### 2. `SettingScreen.kt`
- 添加了權限設定界面
- 包含自啟動權限、後台執行權限和手動啟動服務按鈕
- 每個權限都有詳細的說明文字

### 3. `MainActivity.kt`
- 在應用啟動時自動啟動後台服務

## 權限說明

### 自啟動權限
- **目的**：確保應用在設備重啟後自動啟動
- **設定方式**：點擊「設定自啟動權限」按鈕，根據不同廠商系統會跳轉到相應的設定頁面
- **支援系統**：小米 MIUI、華為 EMUI、OPPO ColorOS、vivo FuntouchOS、三星 OneUI

### 後台執行權限
- **目的**：防止系統在背景中關閉應用
- **設定方式**：點擊「設定後台執行權限」按鈕，跳轉到電池優化設定頁面
- **要求**：忽略電池優化

### 手動啟動服務
- **目的**：手動啟動後台服務
- **使用場景**：當自動啟動失敗時，可以手動啟動服務

## 使用說明

1. **首次使用**：
   - 打開設定頁面
   - 點擊「設定自啟動權限」按鈕
   - 在系統設定中開啟自啟動權限
   - 點擊「設定後台執行權限」按鈕
   - 在電池優化設定中忽略此應用

2. **日常使用**：
   - 應用會在開機時自動啟動
   - 後台服務會持續運行
   - 如果發現服務停止，可以手動點擊「啟動後台服務」

3. **故障排除**：
   - 如果應用在重啟後沒有自動啟動，檢查自啟動權限設定
   - 如果應用在背景中被關閉，檢查電池優化設定
   - 可以手動啟動後台服務來恢復功能

## 技術細節

### 服務生命週期
- `BackgroundService` 使用 `START_STICKY` 標誌，確保服務被殺死後自動重啟
- 每30秒檢查一次服務狀態
- 使用 WakeLock 保持服務運行（最多10分鐘）

### 權限檢查
- 定期檢查電池優化權限狀態
- 自啟動權限狀態暫時設為已設定（實際檢查需要廠商特定API）

### 兼容性
- 支援 Android 6.0 (API 23) 及以上版本
- 針對不同廠商系統提供特定的自啟動設定入口

## 注意事項

1. **電池消耗**：後台服務會增加電池消耗，但這是確保功能正常運行所必需的
2. **系統限制**：某些系統可能會限制後台應用運行，需要用戶手動設定
3. **權限要求**：用戶需要手動授予相關權限，應用無法自動獲取
4. **廠商差異**：不同廠商的系統設定頁面可能不同，需要用戶根據提示操作
