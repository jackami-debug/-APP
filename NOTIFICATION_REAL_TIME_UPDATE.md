# 通知訊息能量條即時更新優化

## 優化目標
讓通知訊息的能量條能夠即時更新能量狀態，包括：
- 使用壞習慣 APP 1分鐘後能量條同步主畫面能量條一起減1
- 使用好習慣 APP 時即時顯示能量條的更新

## 主要修改內容

### 1. EnergyStatusNotifier.kt 優化
- **即時更新支援**：將 `setOnlyAlertOnce(false)` 允許通知即時更新
- **持續通知**：將 `setOngoing(true)` 設為持續通知，避免被滑除
- **動態顏色**：根據能量狀態動態調整通知顏色
  - 能量歸零：紅色
  - 能量偏低（<30%）：橙色
  - 能量充足：綠色
- **動態標題**：根據能量狀態顯示不同的標題文字

### 2. MainActivity.kt 優化
- **即時監聽**：在 `onCreate` 時立即開始通知監聽
- **持續監聽**：在 `onPause` 時保持監聽，確保背景也能更新
- **狀態管理**：添加 `isNotificationMonitoringActive` 避免重複監聽

### 3. EnergyMonitorService.kt 優化
- **每秒檢查**：監控間隔設為 1 秒，提高即時性
- **好習慣即時累積**：新增 `checkGoodHabitEnergyAccumulation` 方法
  - 每秒檢查好習慣 App 的能量累積
  - 支援小數比例的能量累積
  - 避免 ratio < 1 被截斷為 0 的問題

### 4. EnergyViewModel.kt 優化
- **即時日誌**：添加能量更新的日誌記錄
- **即時通知**：確保能量變化能夠即時反映到 UI 和通知系統

### 5. 顏色資源添加
在 `colors.xml` 中添加通知所需的顏色：
- `red_500`：能量歸零時的紅色
- `orange_500`：能量偏低時的橙色
- `green_500`：能量充足時的綠色

## 技術實現細節

### 即時更新機制
1. **EnergyMonitorService** 每秒檢查當前 App 使用情況
2. **壞習慣 App**：每分鐘扣除 1 點能量，即時更新
3. **好習慣 App**：每秒累積小數能量，達到整數時即時增加
4. **StateFlow** 將能量變化即時傳遞給所有監聽者
5. **MainActivity** 監聽能量變化並即時更新通知

### 通知持續性
- 使用 `setOngoing(true)` 確保通知不會被意外滑除
- 在應用進入背景時保持監聽，確保通知持續更新
- 使用 `setOnlyAlertOnce(false)` 允許通知內容即時更新

### 能量計算精確性
- 壞習慣 App：精確的 1:1 比例（每分鐘扣除 1 點）
- 好習慣 App：支援小數比例，避免截斷誤差
- 使用殘值累積機制確保小數能量的精確計算

## 測試驗證
創建了 `EnergyNotificationTest.kt` 來驗證：
- 能量變化時通知更新
- 即時監控功能
- 好習慣能量累積
- 壞習慣能量扣除

## 使用效果
- 通知能量條會即時反映當前能量狀態
- 使用壞習慣 App 時，能量會即時減少並在通知中顯示
- 使用好習慣 App 時，能量會即時增加並在通知中顯示
- 通知顏色和標題會根據能量狀態動態變化
- 通知會持續顯示，不會被意外滑除
