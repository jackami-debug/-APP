# 密碼設定功能實現總結

## 功能概述
在設定頁面添加了密碼設定功能，允許用戶設定6位數字密碼來保護應用程式。

## 實現的功能

### 1. 密碼設定按鈕
- 位置：設定頁面頂部
- 功能：點擊後彈出密碼設定對話框
- 狀態顯示：
  - 未設定密碼時：顯示"設定密碼"（藍色按鈕）
  - 已設定密碼時：顯示"修改密碼"（綠色按鈕）

### 2. 密碼設定對話框
- 包含兩個輸入框：密碼和確認密碼
- 驗證功能：
  - 只允許輸入6位數字
  - 兩次輸入必須一致
  - 實時驗證和錯誤提示
- 安全功能：
  - 密碼隱藏/顯示切換
  - 數字鍵盤輸入

### 3. 清除密碼功能
- 僅在已設定密碼時顯示"清除密碼"按鈕
- 點擊後彈出確認對話框
- 確認後永久清除密碼

### 4. 密碼管理類 (PasswordManager)
- 安全的密碼儲存：
  - 使用 Android Keystore 加密
  - AES/GCM 加密算法
  - Base64 編碼儲存
- 功能方法：
  - `setPassword(password: String)`: 設定密碼
  - `verifyPassword(password: String)`: 驗證密碼
  - `hasPassword()`: 檢查是否已設定密碼
  - `clearPassword()`: 清除密碼

### 5. 密碼驗證對話框
- 可重用的驗證組件
- 用於需要密碼驗證的其他功能

## 技術實現

### 安全性
- 使用 Android Keystore 進行密鑰管理
- AES/GCM 加密算法保護密碼雜湊
- 密碼雜湊後再加密儲存
- SharedPreferences 安全儲存

### UI/UX 設計
- Material Design 3 風格
- 響應式設計
- 實時驗證和錯誤提示
- 直觀的狀態指示

### 代碼結構
- 單例模式的 PasswordManager
- 可重用的對話框組件
- 清晰的狀態管理
- 錯誤處理機制

## 使用方法

1. 進入設定頁面
2. 點擊"設定密碼"按鈕
3. 在對話框中輸入6位數字密碼
4. 再次輸入確認密碼
5. 點擊"確定"完成設定

## 注意事項

- 密碼必須是6位數字
- 設定後無法復原，只能重新設定
- 密碼使用加密儲存，確保安全性
- 清除密碼操作不可逆

## 未來擴展

- 可以添加密碼強度檢查
- 支援指紋/臉部識別驗證
- 添加密碼重設功能
- 整合到應用鎖定功能中
